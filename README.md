# KadouKanri

案件ごとに稼働時間を記録するツールです。

## できること

- 案件を選んでタイマーを開始・停止し、稼働時間を記録する
- 別の案件に切り替えると、自動で前の案件を停止して新しい案件の計測を開始する
- 案件を自由に追加・名前変更できる
- やらなくなった案件はアーカイブして一覧から非表示にできる（データは残る）
- 稼働記録を日付別・月別・案件別に確認できる
- 記録した稼働時間を後から編集・削除できる

## 起動方法

```bash
docker compose up -d
```

ブラウザで http://localhost:5000 を開く。
